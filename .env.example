# Paste output of `npm run gen-auth-hash -- '<passphrase>'` (SHA-256 hex)
VITE_AUTH_HASH_HEX=

# Optional: local development bypass (never ship to production bundles)
VITE_ALLOW_UNAUTH=false

# Override Vite base URL (normally set in CI/GitHub Actions for Pages)
VITE_PAGES_BASE=/
